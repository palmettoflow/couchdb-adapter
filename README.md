# Palmetto Flow CouchDB Adapter

This adapter uses a couchdb database a the commit log.

## Install

```
npm i pf-couchdb-adapter
```

## Usage

```
var palmetto = require('pf-couchdb-adapter')
var ee = palmetto({
    endpoint: '',
    app: ''
  })
```
